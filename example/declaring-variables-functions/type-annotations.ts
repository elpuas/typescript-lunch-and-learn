// Declaring a Variable with a Type Annotation
let newMessage: string = "Hello World";

// Declaring a Function with a Type Annotation
function add(x: number, y: number): number {
    return x + y;
}
