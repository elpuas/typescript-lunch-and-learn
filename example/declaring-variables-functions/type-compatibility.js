"use strict";
const attributes = {
    column: 2,
    row: 3,
    stackOnMobile: true,
    mobileColumn: 1,
    gap: 10
};
// Destructure the attributes object.
const { column, row, stackOnMobile, mobileColumn, gap } = attributes;
