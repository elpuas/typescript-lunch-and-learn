// Enums
enum Color { Red, Green, Blue }
let c: Color = Color.Green;

// Enums can be initialized with numbers
enum ColorNumbers { Red = 1, Green, Blue }
let d:  ColorNumbers = ColorNumbers.Red;

// Enums can be initialized with strings
enum ColorStrings { Red = "Red", Green = "Green", Blue = "Blue" }
let e: ColorStrings = ColorStrings.Red;

